# McTime API Data Bridge Requirements

# Web Framework
flask>=2.0.0

# Production WSGI Server
gunicorn>=20.1.0

# HTTP Requests
requests>=2.25.0

# Environment Variables (SECURITY)
python-dotenv>=0.19.0

# Email (built-in with Python)
# smtplib, email.mime - included in standard library
# Resend API uses the 'requests' package above (no extra dependency needed)
